# conversation/emotional_engine.py
from personality.emotions import apply_emotion

def apply_emotion(text: str, emotion: str):
    """Applies the current emotional tone to the text."""
    return apply_emotion(text, emotion)
